# ============================================================
# Admin frontend env vars — set in Vercel → Project → Environment.
# Public values only. For local dev use .env.development (localhost-only).
# ============================================================

REACT_APP_API_URL=""
REACT_APP_SOCKET_URL=""
